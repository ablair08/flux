apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-d.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "service-d.name" . }}
    helm.sh/chart: {{ include "service-d.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "service-d.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
